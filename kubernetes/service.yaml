apiVersion: v1
kind: Service
metadata:
  name: my-nginx-service
spec:
  selector:
    app: my-web-app  # हा नाव deployment च्या label शी मॅच होतो
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007  # हा पोर्ट AWS Security Group मध्ये Open ठेवा
  type: NodePort